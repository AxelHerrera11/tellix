# SQL Server (sa password — mínimo 8 chars, mayúscula, minúscula, dígito, símbolo)
SA_PASSWORD=YourStr0ng!Pass

# App database user (lo crea el script TellixDB_SQLServer.sql)
DB_USER=tellix_app
DB_PASS=tu_password_aqui

# JWT
JWT_SECRET=cambia-esto-por-una-clave-de-al-menos-256-bits-de-longitud-minima

# Opcional — solo para desarrollo local sin Docker
# DB_URL=jdbc:sqlserver://localhost:1433;databaseName=TellixDB;encrypt=true;trustServerCertificate=true
# CORS_ORIGINS=http://localhost:5173
